// Scratch file used to probe vi.mock behavior while investigating packet-0018.
// `rm` was denied by the sandbox, so this is neutralized to avoid breaking
// `tsc --noEmit`. Safe to delete manually — it is untracked (`git status`
// will show `src/__scratch__/` as `??`).
export {};
